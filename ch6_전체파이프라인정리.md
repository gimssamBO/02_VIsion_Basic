# 전체 파이프라인 정리 (0장~5장)

- 출처: 비전 시각화 기초 실습 원본 자료(정리)

> 0장부터 5장까지는 하나로 이어지는 컴퓨터비전 실습 파이프라인이다. 가상환경 구축 → GPU 인식 확인 → CNN 이미지 분류 → OpenCV 이미지 처리 → 실시간 카메라 스트리밍 → YOLO 실시간 객체 탐지 순으로 진행하며, 각 장은 앞 장의 결과 위에서 다음 단계로 확장된다.

## 장별 흐름

| 장 | 주제 | 앞 장과의 연결 |
| --- | --- | --- |
| 0장 | 가상환경 구축 | 이후 모든 장에서 쓸 라이브러리 설치 기반을 마련 |
| 1장 | GPU(CUDA) 인식 확인 | 2장 CNN 학습 속도에 직접 영향 |
| 2장 | CNN 이미지 분류(TensorFlow / PyTorch) | 1장에서 확인한 GPU 환경 위에서 학습 진행 |
| 3장 | OpenCV 이미지 기초 | 4장 카메라 실시간 처리의 기반이 되는 단일 이미지 처리 흐름 |
| 4장 | OpenCV 카메라 실시간 처리 | 3장의 처리 흐름을 프레임 단위로 반복 적용 |
| 5장 | YOLO 실시간 객체 탐지 | 3~4장의 카메라 프레임 처리 위에 YOLO 추론 단계 추가 |

## 강조할 핵심 포인트

- GPU(CUDA) 인식 확인 흐름: 1장에서 `torch.cuda.is_available()` 결과를 먼저 점검해야 2장 이후 학습 속도를 예측할 수 있다. 하드웨어(`nvidia-smi`)와 프레임워크 인식 결과가 다를 수 있다는 점을 실습 전에 짚어준다.
- TensorFlow vs PyTorch 구조 차이: GPU 사용 방식(자동 vs 명시적 지정), 학습 루프 작성 방식(`model.fit()` vs 직접 반복문), 모델 저장 형식(`.keras` vs `.pth`)이 서로 다르다. 같은 MNIST 문제를 풀더라도 두 프레임워크의 접근 방식이 다르다는 점을 대비해서 설명한다.
- OpenCV → YOLO 파이프라인 연결: 3장 정지 이미지 처리, 4장 실시간 카메라 프레임 처리, 5장 YOLO 추론까지 하나의 흐름으로 이어진다. 3~4장을 연속해서 진행하는 것이 5장 학습 효과를 높인다.

## 실습 순서 권장

1. 0장 가상환경을 먼저 구축하고 장별 라이브러리를 설치한다.
2. 1장에서 GPU 인식 여부를 확인한 뒤, 결과에 따라 2장 학습 시간이 달라질 수 있음을 안내한다.
3. 2장에서 TensorFlow와 PyTorch 두 구현을 비교하며 CNN 전체 과정(로드~저장~재적용)을 익힌다.
4. 3장과 4장을 연속으로 진행해 OpenCV의 정지 이미지 처리와 실시간 카메라 처리를 모두 다룬다.
5. 5장에서 4장까지의 흐름 위에 YOLO 객체 탐지를 얹어 전체 파이프라인을 완성한다.
